<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title>Orders</title>
  </head>
  <body>
    <div id="basket">
      <table class="table table-sm table-striped">
        <thead>
          <tr>
            <th>Product name</th>
            <th>Price</th>
            <th>Qty</th>
          </tr>
        </thead>
        <tbody>
          <% total_price = 0 %>
            <% total_qty = 0 %>
              <% @items.each do |item| %>
                <tr>
                  <td><%= item[0].title %></td>
                  <td><%= item[0].price %></td>
                  <td><%= item[1] %></td>
                </tr>
                <% total_price += item[0].price * item[1].to_i %>
                  <% total_qty += item[1].to_i %><% end %>

                  <tr>
                    <td class="bg-warning"></td>
                    <td class="bg-warning">Total:
                      <%= total_price %>
                      £</td>
                    <td class="bg-warning"><%= total_qty %></td>
                  </tr>
                </tbody>
              </table>

              <h3>Leave Your details:</h3>

              <form action="/place_order" method="POST" role="form">

                <input type="hidden" name="order[orders_input]" value="<%= @orders_input %>"/>

                <div class="form-group">
                  <input id="name" name="order[name]" type="text" class="form-control" placeholder="Enter Your name">
                </div>
                <div class="form-group">
                  <input id="phone" name="order[phone]" type="text" class="form-control" placeholder="Enter Your Phone number">
                </div>
                <div class="form-group">
                  <textarea id="address" name="order[address]" class="form-control" type="text" rows="2" placeholder="Delivery Address"></textarea>
                </div>

                <button type="submit" class="btn btn-success">Finish your order</button>
                <button class="btn btn-danger" onclick="return cancel_order()">Cancel your order</button>
              </form>
            </div>
          </body>
        </html>
